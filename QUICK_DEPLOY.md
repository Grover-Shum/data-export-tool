# 快速部署指南

## 🚀 Railway部署（最简单，推荐）

### 步骤1：准备代码

```bash
# 1. 将Linux版本的pigeon可执行文件放到 src/tools/ 目录
# 文件名：pigeon_conversation-linux-x64

# 2. 初始化git仓库
git init
git add .
git commit -m "Initial commit"

# 3. 推送到GitHub
git remote add origin https://github.com/your-username/data-export-tool.git
git push -u origin main
```

### 步骤2：部署到Railway

1. 访问 [https://railway.app/](https://railway.app/)
2. 使用GitHub账号登录
3. 点击 **"New Project"**
4. 选择 **"Deploy from GitHub repo"**
5. 选择你的仓库
6. Railway会自动检测Dockerfile并开始部署
7. 等待部署完成（约2-3分钟）

### 步骤3：访问应用

部署完成后，Railway会提供一个域名，格式如：
```
https://data-export-tool-production-xxxx.up.railway.app
```

点击域名即可访问Web界面！

---

## 🎯 Render部署（也很简单）

### 步骤1：准备代码

同Railway的步骤1

### 步骤2：部署到Render

1. 访问 [https://render.com/](https://render.com/)
2. 使用GitHub账号登录
3. 点击 **"New"** → **"Web Service"**
4. 连接你的GitHub仓库
5. Render会自动检测 `render.yaml` 配置
6. 点击 **"Apply"** 开始部署
7. 等待部署完成（约3-5分钟）

### 步骤3：访问应用

部署完成后，Render会提供一个域名，格式如：
```
https://data-export-tool.onrender.com
```

---

## 📝 部署检查清单

在部署前，请确保：

- [ ] 已将 `pigeon_conversation-linux-x64` 放到 `src/tools/` 目录
- [ ] 文件有执行权限（Linux/Mac: `chmod +x src/tools/pigeon_conversation-linux-x64`）
- [ ] 代码已推送到GitHub仓库
- [ ] Dockerfile文件存在于项目根目录

---

## 🔧 常见问题

### Q1: 部署失败，提示找不到可执行文件？

**解决方案：**
确保 `pigeon_conversation-linux-x64` 文件已包含在git仓库中：
```bash
git add src/tools/pigeon_conversation-linux-x64
git commit -m "Add linux binary"
git push
```

### Q2: 部署成功但访问报错？

**解决方案：**
1. 查看应用日志（Railway/Render控制台）
2. 检查健康检查接口：访问 `https://your-domain/api/health`
3. 确认返回正常的JSON响应

### Q3: 导出功能不工作？

**解决方案：**
1. 检查日志中的错误信息
2. 确认pigeon可执行文件是否正确
3. 测试API接口：访问 `https://your-domain/docs`

---

## 🎉 部署成功后

### 测试功能

1. 访问你的应用域名
2. 在输入框输入测试命令：
   ```
   请帮我导出修丽可官方旗舰店店铺ID为8226405从2026年2月9日10点到2026年2月9日17点的数据
   ```
3. 点击"开始导出"
4. 等待文件下载

### 分享给外包同学

将应用域名发送给外包同学，他们就可以直接使用了！

---

## 💰 费用说明

### Railway
- 免费额度：每月$5
- 预计消耗：小型应用约$1-2/月
- 超出后：按量付费

### Render
- 免费额度：750小时/月
- 预计消耗：一个Web服务刚好用完免费额度
- 超出后：$7/月起

---

## 🆘 需要帮助？

如果遇到问题：
1. 查看应用日志
2. 检查API文档：`https://your-domain/docs`
3. 测试健康检查：`https://your-domain/api/health`
